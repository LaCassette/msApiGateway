module.exports = class AuthController {
    async login(opts) {
        // logic for login
    }

    async register(opts) {
        // logic for register
    }

    async check(opts) {
        // login for checking user
    }
}